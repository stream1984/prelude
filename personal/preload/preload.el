;; 解决启动卡顿问题，升级Emacs到25应该可以解决问题
(setq tramp-ssh-controlmaster-options)

;;设置command键为M-
;; (setq mac-option-key-is-meta nil
;;       mac-command-key-is-meta t
;;       mac-command-modifier 'meta)

;;启动时全屏
(toggle-frame-maximized)
